---
context: default
phase: N/A
task: Entity Resolution & Graph — runtime fixes & debugging
host_tool: opencode
status: in_progress
last_updated: 2026-07-23 19:34
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Catalyst NoSQL `getItems` / `fetchItem` / `queryTable` return different data formats — must verify `loadDoc` API call actually returns PersonMaster documents (debug logging added, check Catalyst Console logs)
- [ ] CONSTRAINT: Cache `exclusive_start_key` format may not match Catalyst NoSQL expectations — `loadNodes` only returns 100 nodes instead of 679+ (increase `limit` or fix pagination key format)

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Cross-function relative requires | `require('../entity-matching-engine/...')` fails because Catalyst bundles each function independently | blocking | Always copy shared deps into `lib/` subdirectory before deploying |
| Env var wipe on deploy | `catalyst deploy` overwrites Catalyst Console env variables | blocking | Batch all changes into a single deploy; re-add QUICKML_TOKEN before testing |
| Assumed NoSQL API parity | `fetchItem`, `getItems`, `queryTable` have different response formats — don't assume one works like another | blocking | Always test API call with debug logging before relying on it |
| PersonMaster data source_id vs row_id | PersonMaster documents store prefixed app IDs in `source_id` field, but graphService was checking `row_id` | blocking | ALWAYS check the actual NoSQL document fields before writing lookup logic |

<current_state>
Working on Phase 2 (Entity Resolution & Graph Visualization) runtime fixes. All 17 Catalyst functions deploy successfully. Core conversational AI works. Dashboard works. Graph visualization has 3 remaining issues:

1. **BFS traversal returns 0 nodes** — `loadDoc` in `bfs.js` can't find PersonMaster documents even though the cache has them. Debug logging added; check Catalyst Console logs for `[bfs]` output.
2. **Cache only loads 100 nodes** — `loadNodes` pagination with `exclusive_start_key` might have wrong format. Only 100 of 679+ PersonMaster documents cached.
3. **0 edges generated** — personmaster-writer resolve wrote 0 confirmed/unconfirmed edges. Could be data-limited (no co-accused in synthetic data) or edge generation code has silent error.
</current_state>

<completed_work>

Completed This Session:
- **graph-service-api**: Bundled 14 lib/ files with updated require paths; added express/helmet to package.json
- **graph-service-api**: Fixed validators.js to accept numeric IDs + PM_XXXXXX
- **graph-service-api**: Fixed graphService.js async chain (personExists → await _ensureLoaded)
- **graph-service-api**: Fixed resolveSourceRecord to index both row_id and source_id (+ unprefixed variants)
- **graph-service-api**: Added cache-info diagnostic endpoint with dump=true
- **graph-service-api**: Changed loadDoc from fetchItem to getItems
- **personmaster-writer**: Bundled lib/entity-matching-engine/ (5 files), updated index.js requires
- **sync-incremental**: Bundled lib/entity-matching-engine/ + lib/personmaster-writer/, updated incrementalResolver.js + index.js
- **Created package.json + catalyst-config.json** for personmaster-writer, sync-incremental, pm-migration, sync-full-job
- **Fixed stub exports**: pm-migration, sync-full-job changed from exports.handler to module.exports
- **Dashboard**: All 7 endpoints tested and return `{label, value}` data
- **Frontend**: GridLayout → single-column, ChartCard min-h-[360px], DashboardView ResizeObserver
- **All 178 frontend tests pass** (28 files)
- **personmaster-writer resolve**: 679 docs updated, 1497 persons processed, 1405 clusters (1321 singles)
</completed_work>

<remaining_work>

Remaining:
- Debug BFS traversal loadDoc → check Catalyst Console logs for `[bfs]` output from debug logging
- Fix cache pagination (100 vs 679+ nodes) → check exclusive_start_key format
- Investigate why 0 edges generated → check if data has co-accused, or edge code silently fails
- Re-add QUICKML_TOKEN after deploy
- Verify full-stack E2E flow: dashboard charts + person graph
</remaining_work>

<decisions_made>

- **Bundle cross-function deps**: Copy shared files into `lib/` instead of relative requires
- **Index both source_id and row_id**: PersonMaster source_records use `source_id` field (not `row_id`)
- **Use getItems instead of fetchItem**: fetchItem response format may differ from Catalyst expectations
- **Copy not symlink**: Catalyst zip deployment doesn't support symlinks
</decisions_made>

<blockers>

- **BFS loadDoc returns null**: GetItems/fetchItem API calls not returning PersonMaster documents. Debug logging added. Check Catalyst Console logs for `[bfs] Error loading` or `[bfs] getItems result`.
- **Cache only 100 nodes**: Pagination key format may be wrong. `exclusive_start_key` uses `NoSQLMarshall.makeString()` but `queryTable` query uses `NoSQLItem.from()`. These might produce incompatible key formats.
- **0 edges from resolve**: personmaster-writer resolve processed 1497 persons across 1405 clusters but wrote 0 edges. 84 multi-person clusters exist but no CO_ACCUSED/ACCUSED_TO_VICTIM edges. Either: (a) data has no co-accused in same case, or (b) edge generation silently fails.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state
2. `functions/graph-service-api/lib/bfs.js` — loadDoc debug logging at lines 20-38
3. `functions/graph-service-api/lib/graphService.js` — _getSourceRecordIndex at line 117
4. `functions/graph-service-api/lib/graphRepository.js` — loadNodes pagination at line 34-103
5. `functions/personmaster-writer/index.js` — persistClusters edge gen at line 514-562

## Critical Anti-Patterns (do NOT repeat)

- **Don't assume NoSQL API parity**: `fetchItem`, `getItems`, and `queryTable` return different response formats. Always test before relying on a specific API.
- **Check actual document fields**: PersonMaster docs use `source_id`, not `row_id`. Verify field names against actual NoSQL documents.
- **Single deploy per batch**: Each `catalyst deploy` wipes env vars. Batch all changes.

## Infrastructure State

- **Branch**: `phase2-dashboard` (NOT pushed — 14 commits ahead of origin/phase2-dashboard)
- **Origin**: `origin/phase2-dashboard` at commit 381cf19
- **Catalyst deployed**: All 17 functions deployed at `https://datathon2026-60073929329.development.catalystserverless.in/server/`
- **Last git commit**: `ee8d239 test(02-04): add tests for HotspotMapView and MessageBubble integration`
- **QUICKML_TOKEN**: Needs re-adding after last deploy (wiped)
- **Client**: Tests pass (npm test — all 178 pass)

<context>
This session focused on fixing MODULE_NOT_FOUND runtime errors in the entity resolution + graph subsystem functions. The core conversational AI functions (pipeline, classifier, etc.) were already working from Phase 1.

Major effort went into the cross-function dependency bundling pattern:
1. Identified the bug: sibling-directory relative requires fail because Catalyst bundles each function independently
2. Fix: Copy required files into a `lib/` subdirectory within each function
3. Applied to: graph-service-api, personmaster-writer, sync-incremental
4. Also needed: package.json + catalyst-config.json (many entity functions were missing these)

After deployment, personmaster-writer resolve ran successfully (679 docs updated, 1497 persons processed). But:
- No edges were written (0 confirmed, 0 unconfirmed)
- The graph-service-api BFS traversal returns 0 nodes

The BFS issue is puzzling — the cache loads nodes (via queryTable) but loadDoc/getItems doesn't find them. Added debug logging to the BFS — check Catalyst Console logs.

Cache only loads 100 nodes while 679+ exist — the exclusive_start_key format might be wrong (NoSQLMarshall.makeString vs NoSQLItem.from).

The 0-edge issue: 84 multi-person clusters exist (1405 - 1321 = 84). Either data has no co-accused in same case, or edge generation silently catches an error.

Next steps: Check Catalyst logs for [bfs] debug output, verify getItems API, fix cache pagination, investigate edge generation.
</context>

<next_action>
Start with: Check Catalyst Console logs for the graph-service-api function (look for [bfs] prefix). The debug logging added to loadDoc in bfs.js will show exactly what getItems returns. If it shows `"dataLen":0`, the API format is wrong. If shows errors, fix the API call. If the cache only has 100 nodes, fix the exclusive_start_key format in graphRepository.js.

Specific commands:
```bash
# Test cache info with diagnostics
curl "https://datathon2026-60073929329.development.catalystserverless.in/server/graph-service-api/cache-info?dump=true"

# Test graph endpoint
curl "https://datathon2026-60073929329.development.catalystserverless.in/server/graph-service-api/person/1/graph?format=debug"
```
</next_action>
